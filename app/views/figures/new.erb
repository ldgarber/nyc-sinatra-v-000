<h1>New Figure</h1>

<form action="/figures" method="POST">
  <p>Figure Name:<input type="text" name="figure[name]" id="figure_name"></input></p>

  <p>Select Title(s): </p>
  <% @titles.each do |t| %>
    <input type="checkbox" name="figure[title_ids][]" id="title_<%=t.id%>" value="<%=t.id%>" ><%=t.name%></input>  
  <%end%>
  <p>Or, create a new title: </p>
  <input type="text" name="title[name]" id="new_title"></input>

  <p>Select a Landmark: </p>
  <% @landmarks.each do |l| %>
    <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%=l.id%>" value="<%=l.id%>"><%=l.name%></input>
  <%end%>

  <p> Or, create a new landmark: </p>
  <input type="text" id="landmark[name]" name="new_landmark"></input>
  
  <input type="submit" value="Create New Figure"></input>
</form>
